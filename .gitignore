# Dependencies & build output
node_modules/
web/node_modules/
web/.next/
web/out/
web/build/

# Secrets — never commit (also listed in .cursorignore so agents cannot read them)
.env
.env.*
!.env.example
!**/.env.example
web/.env
web/.env.*
!web/.env.example
officialconnect_config.json
*.pem
credentials.json
service-account*.json

# Local / IDE
.DS_Store
Thumbs.db
.vercel
*.log

# Optional: keep example config in repo
!officialconnect_config.example.json
.env*
